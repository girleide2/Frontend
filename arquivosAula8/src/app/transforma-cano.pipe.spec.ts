import { TransformaCanoPipe } from './transforma-cano.pipe';

describe('TransformaCanoPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformaCanoPipe();
    expect(pipe).toBeTruthy();
  });
});
